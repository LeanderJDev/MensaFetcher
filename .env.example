# Example SMTP configuration for MensaFetcher
# Copy this file to `.env` and fill in real values (do NOT commit secrets).

SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_smtp_user@example.com
SMTP_PASS=your_smtp_password
SMTP_USE_SSL=0

MAIL_FROM=you@example.com
MAIL_TO=you@example.com

# Notes:
# - Set SMTP_USE_SSL=1 to use SMTPS (port 465) or leave 0 and allow STARTTLS on 587.
# - MAIL_TO may contain multiple recipients separated by commas.
